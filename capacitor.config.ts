import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.fast.inventory.crm',
  appName: 'Fast Inventory CRM',
  webDir: 'dist'
};

export default config;
